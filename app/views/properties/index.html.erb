<%= link_to "crear nueva propiedad", new_property_path %>
<br/>
<br/>
<%= form_with url: search_number_toilets_path, remote: true, method: :get do |f| %>
    <%= f.label "buscar por cantidad de baños" %>
    <%= f.number_field :number_toilets %>
    <%= f.submit "buscar" %>
 <% end %>
   <br/>
 <%= form_with url: search_number_rooms_path, remote: true, method: :get do |f| %>
    <%= f.label "buscar por cantidad de habitaciones" %>
    <%= f.number_field :number_rooms %>
    <%= f.submit "buscar" %>
 <% end %>
 <br/>
 <%= form_with url: search_address_path, remote: true, method: :get do |f| %>
    <%= f.label "buscar por direccion" %>
    <%= f.text_field :address %>
    <%= f.submit "buscar" %>
 <% end %>

<%= render @properties %>

<%= will_paginate @properties %>

<% if params[:number_toilets] || params[:number_rooms] || params[:address] %>
   <%= link_to "ver todas las propiedades", root_path %>
<% end %>